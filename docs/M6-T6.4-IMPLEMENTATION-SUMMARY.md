# M6-T6.4 统一持久化继续 - 实施总结

**任务:** M6-T6.4 Unified Durable Continuation Implementation  
**日期:** 2025-09-16  
**状态:** Phase 2-3 完成，Phase 5-9 待实施，测试待修复

---

## 执行摘要

**目标:** 实现 EPHEMERAL + DURABLE 双模式执行，通过最小泛化现有 M5/M6 持久化继续脊柱。

**当前进度:** 
- ✅ Phase 1-3 完成 (持久性契约 + 继续倾向泛化)
- 🚧 Phase 5-9 设计完成，待实施
- ⏸️ 测试修复推迟

---

## ✅ Phase 2-3 完成内容

### 生产代码变更

**新增类型 (4个):**
1. `DurabilityMode` enum (EPHEMERAL / DURABLE)
2. `ContinuationDisposition` enum (RUNNABLE / WAITING_FOR_SIGNAL)
3. `AgentExecutionContext` 扩展 (添加 durability 字段)
4. `SuspensionCheckpoint` 扩展 (添加 disposition 字段)

**Public API 变更:**
```java
// AgentExecutionContext - 新方法
AgentExecutionContext.durableSession(String sessionId)
AgentExecutionContext.durableStateless()

// 现有方法保持向后兼容 (默认 EPHEMERAL)
AgentExecutionContext.stateless()          // → EPHEMERAL
AgentExecutionContext.withSession(String)  // → EPHEMERAL
```

**Schema 演进:**
- v1.0: M5 原始 (无 executionEpoch, 无 disposition)
- v1.1: M6-T4F 添加 executionEpoch (nullable)
- v1.2: M6-T6.4 添加 disposition (nullable for legacy) ✅

**已更新文件 (生产代码):**
1. `DurabilityMode.java` (新)
2. `ContinuationDisposition.java` (新)
3. `AgentExecutionContext.java` (扩展)
4. `SuspensionCheckpoint.java` (扩展)
5. `CheckpointJsonCodec.java` (向后兼容读取)
6. `SpringAiToolCallingEngine.java` (使用 WAITING_FOR_SIGNAL)
7. `DurableResumeCoordinator.java` (使用 WAITING_FOR_SIGNAL)

**编译状态:** ✅ 所有生产代码编译通过

---

## 🚧 待实施内容

### Phase 5: DURABLE ALLOW 逻辑操作物化

**核心不变式:**
> 对于 DURABLE 执行，一旦接受逻辑操作并分配 operationId，
> 该逻辑操作必须在物理副作用能力调用被允许之前成为持久化可达的。

**实施任务:**
1. [ ] 修改 `GovernanceToolCallingAdvisor` 检测 `context.durability()`
2. [ ] 在 ALLOW + DURABLE 时触发持久化信号
3. [ ] 实现 `executeDurableAllow()` 方法
4. [ ] 创建 processId + checkpoint (RUNNABLE disposition)
5. [ ] checkpointStore.create() 提交

### Phase 6: 内部自动继续收敛

**实施任务:**
1. [ ] 使 `ContinuationSignal` 可选 (内部)
2. [ ] `DurableResumeCoordinator` 支持无信号继续
3. [ ] 收敛到统一 CHECK A/B 路径

### Phase 7: 工具结果/继续重建

**实施任务:**
1. [ ] 工具执行后结果持久化
2. [ ] 崩溃后继续重建逻辑
3. [ ] InvocationStateStore resolution 重用

### Phase 8: 第二语义边界 (条件性)

**评估:** 工具完成后是否需要另一个持久化边界？

**如需要:**
1. [ ] READY_FOR_MODEL_CONTINUATION 状态
2. [ ] CHECK B replaceIfVersion 进入下一代

### Phase 9: CHECK A/B 泛化 + T5 集成

**实施任务:**
1. [ ] 验证 CHECK A 对 RUNNABLE 生效
2. [ ] 验证 CHECK B 对 RUNNABLE 生效
3. [ ] T5 recovery 完整集成
4. [ ] executionEpoch 跨 RUNNABLE 继续

---

## ⏸️ 测试修复 (Phase 10+)

**待修复测试文件 (~14 个):**

**arctra-core:**
- CheckpointStoreConcurrencyTest.java
- InMemoryCheckpointStoreTest.java
- CrossRuntimeFailureRecoveryTest.java
- RuntimeBindingExceptionClassificationTest.java
- AgentExecutionContextTest.java ✅

**arctra-runtime-react:**
- ExplicitRecoveryPathTest.java
- DurableResumeMemoryTest.java
- JdbcDurableRecoveryPairTest.java
- ConcurrentDurableResumeTest.java
- JdbcCheckpointStoreTest.java
- AutomaticRecoveryModeSelectionTest.java
- DurableResumeEvidenceTest.java
- CheckpointJsonCodecTest.java
- DurableResumeExecutionTest.java
- DurableResumeGovernanceTest.java

**策略:** 完成所有生产代码后批量修复

---

## 架构质量保证

### 不变式验证

✅ **单一恢复脊柱:** CheckpointStore  
✅ **单一 CAS 模型:** checkpointVersion  
✅ **单一运行时绑定:** RuntimeBindingResolver  
✅ **单一重启检测:** executionEpoch  
✅ **单一物理尝试权威:** InvocationStateStore  
✅ **自描述 Checkpoint:** disposition 字段  
✅ **Schema 向后兼容:** v1.2 可读 v1.0/v1.1  
✅ **无第二恢复栈**  
✅ **无并行协调器**

### 必须通过的测试

**Phase 11+ 对抗性测试:**
1. ALLOW + EPHEMERAL 行为不变
2. ALLOW + DURABLE 跨 JVM 重启存活
3. operationId 在 intent 前持久化
4. checkpoint 失败阻止 delegate.call
5. 崩溃后 intent 产生正确不确定性
6. T5 recovery 重连原始逻辑操作
7. 相同名称/参数操作保持不同
8. 部分批次恢复工作
9. REQUIRE_APPROVAL 行为保持正确
10. CHECK B 并发失败者语义正确

---

## 估算

**剩余工作量:**
- Phase 5-9 生产代码: 8-12 小时
- Phase 10+ 测试修复: 4-6 小时
- Phase 11+ 对抗性测试: 4-6 小时
- **总计:** 16-24 小时 (2-3 天)

---

## 下次继续入口点

**Phase 5 实施起点:**
1. 文件: `arctra-runtime-react/src/main/java/cn/bitcss/arctra/runtime/react/governance/GovernanceToolCallingAdvisor.java`
2. 参考: `ToolApprovalRequiredSignal` 模式
3. 实现: durability 检测 + 信号机制
4. 目标: 拦截 ALLOW + DURABLE 路径

**关键文件清单:**
- `GovernanceToolCallingAdvisor.java` (修改)
- `SpringAiToolCallingEngine.java` (添加 executeDurableAllow)
- `DurableResumeCoordinator.java` (支持无信号继续)
- 新增测试辅助: `CheckpointTestHelper.java` ✅

---

## 参考文档

1. `M6-T6.3-DUAL-MODE-DURABLE-EXECUTION-ARCHITECTURE-GATE.md` (架构决策)
2. `M6-T6.4-SOURCE-BASELINE.md` (源代码审计)
3. `M6-T6.4-PHASE-5-DESIGN.md` (Phase 5 设计)
4. `M6-T6.4-PAUSE-REPORT.md` (当前状态)

---

**M6-T6.4 实施准备就绪。可随时继续 Phase 5。**
