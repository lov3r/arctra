# M6-T2.5A-R4 Source Truth Audit

## 1. Precondition Verification

### 1.1 One Resumed Execution Mechanism ✅

```bash
# Old Engine methods
durableContinueWithMessages: 0 occurrences ✅
reconstructAndExecuteApproved: 0 occurrences (outside comments) ✅
reconstructDenialResponses: 0 occurrences (outside comments) ✅
```

**R3 Handler mechanism**:
- SpringAiResumedExecutionHandler: present ✅
- executeResume(): present ✅
- ResumedExecutionOutcome: present ✅

**Verdict**: Exactly ONE active durable resumed-execution mechanism ✅

### 1.2 Provider Boundary Clean ✅

```bash
# ResumedExecutionOutcome.java
org.springframework.ai.* imports: 0 ✅
```

**ResumedExecutionOutcome.GovernanceSuspended**:
```java
record GovernanceSuspended(
    List<PendingToolCall> pendingBatch,  // ✅ Framework-neutral
    List<Evidence> evidences
)
```

**Verdict**: Provider boundary remains clean ✅

---

## 2. Engine Durable Resume Methods

### Current Methods

| Method | Line | Classification | Responsibility |
|--------|------|----------------|----------------|
| `resumeProcess()` | 701 | DURABLE | Entry point, precondition validation, delegation |
| `loadAndValidateCheckpoint()` | 1031 | DURABLE | CHECK A - load & version validation |
| `resolveBinding()` | 1060 | DURABLE | RuntimeBinding resolution |
| `handleResumedExecutionOutcome()` | 770 | COMPOSITION | Switch on outcome, delegate CHECK B |
| `handleDurableReSuspensionFromOutcome()` | 854 | DURABLE | CHECK B re-suspension with events |
| `emitEvent()` | 1061 | DURABLE | Event emission helper |

### Migration Target

**TO COORDINATOR**:
- resumeProcess() (orchestration logic)
- loadAndValidateCheckpoint()
- resolveBinding()
- handleResumedExecutionOutcome() (CHECK B orchestration)
- handleDurableReSuspensionFromOutcome()
- emitEvent() (or duplicate minimal helper)

**STAYS IN ENGINE**:
- Public entry validation only
- Initial execution
- Initial suspension (suspendForApprovalDurable)

---

## 3. Dependencies Analysis

### Engine Current Durable Dependencies

```java
private final CheckpointStore checkpointStore;
private final RuntimeBindingResolver bindingResolver;
private final String runtimeBindingKey;
private final ExecutionEventListener executionEventSink;
private final SpringAiResumedExecutionHandler resumedExecutionHandler;
```

### Coordinator Expected Dependencies

```java
// Required
private final CheckpointStore checkpointStore;
private final RuntimeBindingResolver bindingResolver;
private final ExecutionEventListener eventListener;
private final ResumedExecutionHandler resumedExecutionHandler;

// NOT REQUIRED for resume
// Engine's runtimeBindingKey is for INITIAL suspension only
// Resume uses checkpoint.runtimeBindingKey()
```

---

## 4. R4 Scope

### Move to Coordinator

- CHECK A (load, version validation)
- RuntimeBinding resolution
- Signal validation
- APPROVAL_GRANTED / APPROVAL_REJECTED
- RESUMED
- Resumed execution delegation
- CHECK B completion (deleteIfVersion)
- CHECK B re-suspension (replaceIfVersion)
- CHECKPOINT_CONFLICT
- COMPLETED
- APPROVAL_REQUIRED
- SUSPENDED
- Next checkpoint creation
- AgentProcess creation

### Keep in Engine

- Public entry (resumeProcess)
- Durable mode precondition check
- Initial execution (execute)
- Initial suspension (suspendForApprovalDurable)
- Public API surface

---

## Next: Implementation
